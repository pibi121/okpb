# Рабочие ComfyUI workflow (база)

Дата фиксации: **2026-08-07**

## Файлы

| Файл | Назначение |
|------|------------|
| `still-lora-flux.json` / `Лора+Флакс.json` | Кадр: LoRA + Flux |
| `video-remix-rife.json` / `Wan-Remix-RIFE.json` | Видео: Wan Remix → RIFE ×2 |

Цепочка продукта: **still → Remix → RIFE**. SeedVR2 в базу не входит.

На Metalnode те же JSON лежат в `/work/ComfyUI/user/default/workflows/`.  
В UI: Workflows → открыть `still-lora-flux` / `video-remix-rife` (или Ctrl+F5).
